import React, { useState } from "react";
import styles from "./Navbar.module.scss";

const Navbar = () => {
    return (
        <>
            <nav>
                <h1>Navbar</h1>
            </nav>
        </>
    );
};

export default Navbar;
